# EntryScopeWorkerTest "filter": Archive(EntryScope) -> body -> ResultSink keep: all
main:
  is: Job
  results:
    main:
      class: kotlin.Any
      generics: []
      nullable: true

main.channels/written:
  is: Channel

main.workers/Archive:
  is: EntryScope
  path: build/content-scope/filter/input.tar.gz
  entries: []
  output: main.channels/written

main.workers/Archive.body/Filter:
  is: ScopeFilter
  where: "size > 8"

main.workers/Archive.body/Write:
  is: ContentWriter
  directory: build/content-scope/filter/out

main.workers/collect:
  is: ResultSinkWorker
  input: main.channels/written
  keep: all
