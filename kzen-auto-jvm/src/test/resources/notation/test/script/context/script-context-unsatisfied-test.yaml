# Unsatisfied requires (ScriptContextValidationTest): nothing before the step provides its Context, and this
# document asserts no caller either, so the requirement could never be met — an ERROR, which disables Run.

main:
  is: Script

main.steps/Read:
  is: RequireContextTestStep
