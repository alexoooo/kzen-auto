# The uniform `uses` gate (ScriptContextRuntimeTest): a step declaring `uses:` with nothing bound
# fails at the SPINE, with one framing, before its body runs — instead of at whatever ad-hoc read it happens
# to reach.

main:
  is: Script

main.steps/Read:
  is: RequireContextTestStep
